import { View, Text, Button } from "react-native";
import React from "react";

const Login = () => {
    return (
        <View>
            <Button title="Login" />
        </View>
    );
};

export default Login;
